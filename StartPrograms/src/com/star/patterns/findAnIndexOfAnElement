//Find and Index of an given element in an array.   
   
   public static void main(String[]args) {
       
       //Find an index of an element in an Array.
       Scanner sc = new Scanner(System.in);
       int n=sc.nextInt();
       int [] arr = new int[n];

       for(int i=0;i<arr.length;i++){
           arr[i]=sc.nextInt();
       }
       
       int findElement = sc.nextInt();
       int index=-1;
       for(int i=0;i<arr.length;i++){
           if(arr[i]==findElement){
               index=i;
           }
       }
       
          System.out.println("Element Found on index:"+index);
   }
  
